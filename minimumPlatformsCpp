int findPlatform(int arr[], int dep[], int n){
	vector<int> v(2400,0);
    
	for(int i=0;i<n;i++){
		v[arr[i]]++;
		 v[dep[i]+1]--;
    }
        
    int ans=v[0];
    for(int i=1;i<2400;i++) {
        v[i]+=v[i-1];
        ans=max(ans,v[i]);
     }
        
    return ans;
}
